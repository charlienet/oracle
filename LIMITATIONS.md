# Oracle 驱动限制与注意事项

本文档详细说明了 GORM Oracle 驱动的已知限制、Oracle 数据库特性和使用注意事项。

## 目录

- [Oracle 数据库限制](#oracle-数据库限制)
- [驱动实现限制](#驱动实现限制)
- [GORM 特性支持](#gorm-特性支持)
- [存储过程调用](#存储过程调用)
- [常见问题](#常见问题)

---

## Oracle 数据库限制

### 1. 批量 MERGE 不支持

**限制**：Oracle 不支持多行 VALUES 语法的 MERGE 语句。

**影响**：
- `Association.Append()` 批量添加关联时失败
- `FullSaveAssociations` 批量保存关联时失败

**解决方案**：
```go
// ❌ 错误：批量添加关联
db.Model(&user).Association("Orders").Append([]Order{order1, order2})

// ✅ 正确：逐条添加
for _, order := range orders {
    db.Model(&user).Association("Orders").Append(order)
}
```

### 2. RETURNING 子句限制

**限制**：Oracle 仅支持单行 `RETURNING INTO`，不支持多行批量 RETURNING。

**影响**：
- 批量 INSERT 采用逐行执行，性能较低
- 多行 UPDATE 不启用 RETURNING 回填

**解决方案**：
- 批量插入时驱动自动逐行执行，保证 ID 正确回填
- 如需高性能批量插入，考虑使用 `INSERT ALL` 语法（需手动实现）

### 3. 外键约束限制

**限制**：Oracle 不支持 `ON UPDATE CASCADE` 外键操作。

**影响**：
- `Association.Replace()` 可能失败（尝试将外键设为 NULL）
- `Association.Clear()` 可能失败（NOT NULL 约束）

**解决方案**：
- 使用 `Association.Delete()` 替代 `Clear()`
- 手动管理外键关系

### 4. 数据类型限制

#### 4.1 VARCHAR2 长度限制

size > 4000 的 string 列类型取决于 MAX_STRING_SIZE 探测结果（Initialize 时从 `database_properties` 探测）：

| MAX_STRING_SIZE 探测结果 | VARCHAR2 上限 | size>4000 的 string 列处理方式 |
|--------------------------|--------------|-------------------------------|
| EXTENDED（12c+ 且已启用） | 32767 字节 | `VARCHAR2(≤32767)`，超出降级为 CLOB |
| STANDARD / 未探测（含 11g） | 4000 字节 | 超过自动降级为 CLOB |

**注意**：未探测或探测失败时按 STANDARD 保守处理，size > 4000 的字符串字段使用 CLOB。

#### 4.2 BOOLEAN 类型

| 版本 | 实现方式 |
|------|---------|
| 11g - 20c | NUMBER(1)，自动转换 1/0 |
| 21c+ | 原生 BOOLEAN |

#### 4.3 自增主键

| 版本 | 实现方式 |
|------|---------|
| 11g | 序列 + BEFORE INSERT 触发器 |
| 12c+ | GENERATED BY DEFAULT AS IDENTITY |

### 5. 分页语法

| 版本 | 语法 |
|------|------|
| 11g | ROWNUM 改写 |
| 12c+ | OFFSET n ROWS FETCH NEXT n ROWS ONLY |

### 6. 注释更新

**限制**：Oracle 不支持在 ALTER TABLE 中修改注释。

**实现**：使用 `COMMENT ON COLUMN` 语句单独更新。

**注意**：AutoMigrate 时会自动检测注释变更并更新。

---

## 驱动实现限制

### 1. WHERE 安全检查

驱动会在 UPDATE/DELETE 时检查 WHERE 条件，缺少有效条件时返回错误：

```go
// ❌ 错误：缺少 WHERE 条件
db.Model(&User{}).Update("name", "Bob")
// 返回：missing WHERE condition

// ✅ 正确
db.Model(&User{}).Where("id = ?", 1).Update("name", "Bob")
```

**例外**：软删除条件被视为有效条件。

### 2. 列名大小写

Oracle 返回大写列名，驱动会自动映射回模型定义的大小写。

**注意**：
- 模型中使用小写列名时，驱动会自动处理
- 避免在查询中混用大小写

### 3. 标识符引用

Oracle 保留字会自动加引号，避免语法错误。

**示例**：
```go
type User struct {
    ID   uint   // 正常
    Name string // 正常
    Type string // 自动引用为 "TYPE"（Oracle 保留字）
}
```

### 4. 批量插入性能

由于 Oracle 限制，批量插入采用逐行执行：

```go
// 1000 条记录会执行 1000 次 INSERT
users := make([]User, 1000)
db.Create(&users)
```

**性能优化建议**：
- 使用事务包裹批量操作
- 考虑使用 `INSERT ALL` 语法（需手动实现）
- 对于超大批量，考虑使用 SQL*Loader 或外部工具

### 5. 事务隔离级别

**限制**：事务隔离级别**仅支持默认值（即 READ COMMITTED）**。go-ora 驱动（v2.9.0）的 `BeginTx` 对显式隔离级别有硬限制（connection.go）：`Isolation != 0` 或 `ReadOnly == true` 时直接返回错误（"only support default value for isolation" / "readonly transaction is not supported"）。

**影响**：
- `DB.Transaction(fc, &sql.TxOptions{Isolation: sql.LevelSerializable})` 会报错，事务无法开始
- 注意：**显式**传入 `sql.LevelReadCommitted`（=2，非 0）同样会被拒绝——READ COMMITTED 只能以「不传 TxOptions」的默认形态使用

**解决方案**：
- 使用默认事务：`DB.Transaction(fc)`（Oracle 默认隔离级别即 READ COMMITTED）
- 如需更强一致性，请自行处理：应用级锁、显式 `SELECT ... FOR UPDATE`、物化视图等

**测试**：见 `tests/isolation_test.go`（负向测试固化该驱动限制，未来驱动若支持其他隔离级别，测试会显式失败提示重新评估）。

---

## GORM 特性支持

### 完全支持的特性

- ✅ 基础 CRUD（Create, Read, Update, Delete）
- ✅ 软删除（Soft Delete）
- ✅ 事务（Transaction）
- ✅ 嵌套事务（SavePoint）
- ✅ Hook（Before/After Create/Update/Delete/Query）
- ✅ 关联查询（BelongsTo, HasOne, HasMany, Many2Many）
- ✅ Preload 预加载
- ✅ Joins 预加载（BelongsTo, HasOne, HasMany）
- ✅ OnConflict（Upsert）
- ✅ 序列和自增主键
- ✅ 枚举类型
- ✅ JSON/GOB 序列化器
- ✅ 存储过程调用
- ✅ 游标输出

### 部分支持的特性

| 特性 | 状态 | 说明 |
|------|------|------|
| Association.Append | ⚠️ | 不支持批量，需逐条添加 |
| Association.Replace | ⚠️ | 可能因外键约束失败 |
| Association.Clear | ⚠️ | 可能因 NOT NULL 约束失败 |
| Many2Many Joins | ⚠️ | 部分场景不支持 |

### 不支持的特性

| 特性 | 原因 |
|------|------|
| 批量 MERGE | Oracle 不支持多行 MERGE |
| ON UPDATE CASCADE | Oracle 不支持，使用触发器模拟 |
| 多行 RETURNING | Oracle 仅支持单行 |

---

## 存储过程调用

### 基本用法

驱动提供了本地包装结构，隐藏底层驱动细节：

```go
import (
    oracle "github.com/charlienet/go-oracle"
)

// OUT 参数
var result string
outParam := oracle.OutParam(&result, 100)
db.Exec("BEGIN my_proc(:1); END;", oracle.ToDriverParam(outParam))

// IN OUT 参数
var value int = 5
inOutParam := oracle.InOutParam(&value)
db.Exec("BEGIN my_proc(:1); END;", oracle.ToDriverParam(inOutParam))

// 游标输出
cursor := oracle.NewRefCursor()
db.Exec("BEGIN my_proc(:1); END;", oracle.ToDriverParam(oracle.OutParam(cursor.ToDriverCursor(), 0)))
rows, err := go_ora.WrapRefCursor(context.Background(), sqlDB, cursor.ToDriverCursor())
```

### 参数类型

| 类型 | 说明 | 示例 |
|------|------|------|
| `OutParam(dest, size)` | OUT 参数 | `OutParam(&result, 100)` |
| `InOutParam(dest)` | IN OUT 参数 | `InOutParam(&value)` |
| `InParam(value)` | IN 参数（通常不需要） | `InParam(123)` |
| `NewRefCursor()` | 游标参数 | `NewRefCursor()` |

### 注意事项

1. **VARCHAR2 参数**：必须指定 size，否则 Oracle 报错
2. **游标参数**：需要使用 `go_ora.WrapRefCursor` 包装
3. **驱动绑定**：使用本地包装结构隐藏驱动细节；当前仅支持 go-ora（godror 为路线图项，未接线，见 [Q5](#q5-支持-godror-吗)）

---

## 常见问题

### Q1: 批量插入性能如何？

**A**: 由于 Oracle 限制，批量插入采用逐行执行。1000 条记录大约需要 1-2 秒（取决于网络和数据库性能）。

**优化建议**：
- 使用事务包裹
- 考虑使用 `INSERT ALL` 语法
- 超大批量使用 SQL*Loader

### Q2: 如何处理枚举类型？

**A**: 支持两种枚举类型：

```go
// 1. 底层为 int
type Status int
const (
    StatusPending Status = iota
    StatusActive
    StatusInactive
)

// 2. 底层为 string
type Role string
const (
    RoleAdmin    Role = "admin"
    RoleUser     Role = "user"
    RoleGuest    Role = "guest"
)
```

驱动会自动处理枚举类型的存储和读取。

### Q3: 如何处理 JSON 数据？

**A**: 使用 GORM 的 JSON 序列化器：

```go
type Config struct {
    ID   uint
    Data map[string]interface{} `gorm:"serializer:json"`
}
```

**注意**：
- 小数据（< 4000 字节）使用 VARCHAR2
- 大数据（>= 4000 字节）使用 CLOB

### Q4: 如何处理大文本（CLOB）？

**A**: 自动处理：

```go
type Article struct {
    ID      uint
    Title   string `gorm:"size:100"`
    Content string `gorm:"size:10000"` // 自动使用 CLOB
}
```

### Q5: 支持 godror 吗？

**A**: 当前不支持切换。本驱动仅支持 go-ora（默认驱动名 `"oracle"`），godror 支持为**路线图项**，尚未接线启用：

- `driver_adapter` 中保留的 godror 实现（`driver_adapter/godror.go`，受 `go:build godror` 约束）为**预留代码**，尚未在 `Initialize` 中接线驱动路径；
- 将 `Config.DriverType` 设置为 `driver_adapter.DriverGodror` **不会生效**（`Initialize` 仅按 `DriverName` 打开连接），按此配置使用会 100% 失败。

**当前状态**：仅支持 go-ora。如需 godror 支持，请关注后续版本（路线图）。

### Q6: 如何处理时区？

**A**: 使用 `TIMESTAMP WITH TIME ZONE` 类型：

```go
type Event struct {
    ID        uint
    EventTime time.Time // 自动使用 TIMESTAMP WITH TIME ZONE
}
```

### Q7: 如何处理并发？

**A**: GORM 的 `*gorm.DB` 是并发安全的，可以在多个 goroutine 中共享：

```go
var db *gorm.DB

func init() {
    db, _ = gorm.Open(oracle.Open(dsn), &gorm.Config{})
}

func handler() {
    // 可以在多个 goroutine 中使用 db
    var users []User
    db.Find(&users)
}
```

---

## 性能建议

### 1. 使用连接池

```go
sqlDB, _ := db.DB()
sqlDB.SetMaxIdleConns(10)
sqlDB.SetMaxOpenConns(100)
sqlDB.SetConnMaxLifetime(time.Hour)
```

### 2. 使用事务

```go
db.Transaction(func(tx *gorm.DB) error {
    // 所有操作在同一个事务中
    return nil
})
```

### 3. 批量操作使用事务

```go
db.Transaction(func(tx *gorm.DB) error {
    for _, user := range users {
        tx.Create(&user)
    }
    return nil
})
```

### 4. 预加载关联

```go
// ✅ 推荐：使用 Preload
db.Preload("Orders").Find(&users)

// ❌ 避免：循环查询
for _, user := range users {
    db.Model(&user).Association("Orders").Find(&user.Orders)
}
```

---

## 版本兼容性

| 驱动版本 | GORM 版本 | Go 版本 | Oracle 版本 |
|---------|----------|---------|------------|
| v1.0.x | v1.31.2 | 1.22+ | 11g, 12c, 21c, 23ai |

---

## 反馈与支持

如有问题或建议，请提交 Issue 或 Pull Request。

---

## 不支持的功能

### 1. 批量 MERGE 操作

**原因**：Oracle 不支持多行 VALUES 语法的 MERGE 语句。

**影响的 API**：
- `Association.Append()` - 批量添加关联
- `Association.Replace()` - 替换关联
- `FullSaveAssociations` - 批量保存关联

**当前状态**：这些测试失败是预期行为，因为 Oracle 数据库本身的限制。

**解决方案**：
- 使用循环逐条添加
- 手动实现批量 MERGE 逻辑

### 2. 外键约束的 ON UPDATE 操作

**原因**：Oracle 不支持 `ON UPDATE CASCADE` 外键操作。

**解决方案**：驱动使用触发器模拟 CASCADE/SET NULL 行为。

---

## 序列化问题

### 1. GOB 序列化器

**问题**：GOB 编码的数据在存储到 Oracle 时，读取时无法正确解码。

**错误信息**：
```
failed to unmarshal gob value: "0D7F040102FF8000010C0104000015FF800003036F6E65020374776F0405746872656506"
```

**原因分析**：
- GOB 编码生成的是二进制数据
- Oracle 的 RAW/VARCHAR2 类型在存储和读取时可能存在编码问题
- go-ora 驱动在处理二进制数据时可能与 GOB 的编码格式不兼容

**建议**：
- 使用 JSON 序列化器替代 GOB
- 如果必须使用 GOB，考虑使用 BLOB 类型存储

**测试状态**：`TestSerializer_GOB` 失败

### 2. JSON 序列化器

**状态**：✅ 完全支持

所有 JSON 序列化测试通过：
- `TestSerializer_JSON` - 基础 JSON
- `TestSerializer_JSON_CLOB` - CLOB 存储
- `TestSerializer_JSON_Struct` - 结构体序列化
- `TestSerializer_JSON_Update` - 更新操作
- `TestSerializer_JSON_Slice` - 数组序列化
- `TestSerializer_JSON_Empty` - 空值处理
- `TestSerializer_JSON_LargeValue` - 大值处理

**建议**：优先使用 JSON 序列化器，它在 Oracle 上工作良好。

### 3. UnixTime 序列化器

**状态**：✅ 完全支持

`gorm:"serializer:unixtime"` 的 int64 字段（Unix 秒）序列化为 `time.Time` 存储，列型映射为 TIMESTAMP WITH TIME ZONE（与 `time.Time` 字段一致），读取时转回 int64：

```go
type Event struct {
    ID        uint
    Timestamp int64 `gorm:"serializer:unixtime"` // 存储为 TIMESTAMP WITH TIME ZONE
}
```

**说明**：gorm 的 UnixSecondSerializer 语义为「int64 字段 ↔ time.Time 值」（Value 返回 `time.Time`、Scan 经 `sql.NullTime` 接收），因此列型必须是 TIMESTAMP 系列；若按 int64 默认的 NUMBER 映射会报 ORA-00932（expected NUMBER got TIMESTAMP）且读取时 int64→time.Time 转换失败。此前记录的「不支持」即源于此类型映射错误，现已修复。

**测试**：`TestSerializer_UnixTime`（tests/serializer_test.go）验证端到端往返。

---

## 测试统计（Oracle 12c）

- **总测试数**：148
- **通过**：141（95.3%）
- **失败**：7（4.7%）

**失败测试分类**：
- Association 相关：5 个（Oracle 限制）
- 序列化相关：1 个（GOB）

**结论**：驱动的核心功能完全可用，失败的测试主要是由于 Oracle 数据库本身的限制或序列化器的兼容性问题。
